{
  "judge": {
    "comparator_timeout_seconds": 3600,
    "audit_timeout_seconds": 300,
    "timing_timeout_seconds": 1800,
    "perf_phase_budget_seconds": 10800,
    "timing_reps": 3,
    "max_submission_bytes": 1048576,
    "max_submission_files": 1,
    "max_tool_output_bytes": 1048576
  },
  "perf_defaults": {
    "count": 4,
    "spacing": "geometric",
    "jitter": 0.15,
    "note": "Compatibility defaults for legacy evaluation-policy-v1 records; not used by the eight Stage 1 problems. The judge-level perf_phase_budget_seconds is likewise legacy-only. Stage 1 problems define independent grouped cases in their fixed config.json files, and their evaluation-policy-v2 cohorts seal perf_phase_budget_seconds=0 so one case cannot consume another case's configured time allowance. Official evaluation requires a hidden PERF_SEED and seals the resolved schedule into the cohort policy; an unseeded schedule is for deterministic local development only."
  },
  "timing": {
    "metric": "wall_time",
    "remote_protocol": "KTP/3",
    "measurement_contract": "kernel-replay-v2",
    "correctness_boundary": "full-closure-replay-v1",
    "performance_boundary": "target-declaration-replay-v1",
    "target_proof_encoding": "direct-rfl-v1-experimental",
    "legacy_remote_replay_memory_mb": 4096,
    "note": "Local dev uses wall_time. Stage 1 official cohorts use in-container local-v2 perf_instructions on the pinned Linux PMU host. KTP/3 is non-official validation only. Metrics are ranked separately. Correctness and target declarations use three replays and record each median. Process startup, export parsing, and per-input dependency preload are excluded; exact output-literal checking remains included. KTP/3 binds each remote target replay to the per-problem evaluation.memory_mb bound and reports memory exhaustion during that replay as a case resource-limit outcome; official jobs instead use the per-problem evaluation.memory_mb container limit. Memory exhaustion during timed correctness replay leaves an otherwise accepted submission unscored. Other protocol or boundary versions are legacy/unscored."
  },
  "scoring": {
    "contract": "eight independent problem leaderboards; full-plan passes score 100 and rank by configured target work or combined work; any failed case gives 0 points and infinite cost, with all such failures tied",
    "policy_source": "evaluation/problems/<id>/config.json for all eight Stage 1 problems. The evaluation block (grouped-evaluation-v1) is sealed into each evaluation-policy-v2 cohort.",
    "total_work": "full_correctness_closure_replay_median + sum(successful_target_declaration_replay_medians)",
    "metric_groups": [
      "perf_instructions",
      "wall_time"
    ],
    "report_only": [
      "alpha",
      "beta"
    ],
    "note": "There is no cross-problem total. Each scored problem publishes its three input groups, hidden-case count, resource limits, and target-work or combined-work policy. Full-plan passes score 100 points; any failed case gives an otherwise scoreable submission 0 points and infinite ranking cost. No partial profile or extra proof-cost tie-break applies. All work terms must use the verdict's one metric, protocol, measurement contract, boundary versions, and target-proof encoding. Alpha and beta never affect rank. Rankings never cross problem, metric, cohort, or measurement version."
  },
  "sandbox": {
    "mode": "none",
    "note": "'none' = local dev via the landrun pass-through shim (NO sandboxing). 'container' asserts a real landrun is present and is set by the Docker image. Resource/isolation limits (memory, cpus, pids, network, non-root, kill-all-on-exit) are ENFORCED BY scripts/run_isolated.sh and the container runtime, not by the judge process. Each scored problem declares evaluation.memory_mb in its fixed config.json. The wrapper enforces that job-wide cap with zero extra swap; the judge checks the cgroup values and seals the limit into the cohort. Matrix permanent uses 8192 MiB (8 GiB); the other seven scored problems retain transitional 4096 MiB entries pending organizer confirmation. Official-host acceptance remains required for all problems. Memory changes require a new cohort and rescoring the comparison set.",
    "cpus": 2
  },
  "toolchain": {
    "lean": "leanprover/lean4:v4.33.1",
    "comparator_rev": "3927ad383f208ae977c340a91c48ac9b497d2097",
    "lean4export_rev": "15f6055e299ad5b89345e533cc2192f4cc00f659"
  }
}
